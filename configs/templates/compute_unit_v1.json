{
  "description": "Single Compute Unit blueprint (CU = ScalarCache + L1Cache, both CacheTLM. v2.2 简化版; SM rewrite 后, 完整 CU = StreamingMultiprocessorTLM (12 ChStream 子模块 + IComputeDevice 接口))",
  "p2_placeholder": "P2 注释: v2.2 仅有 CacheTLM. Task 14 (SM 重构) 后, 完整 CU 蓝图应替换为 StreamingMultiprocessorTLM + 12 子模块, 此处保持向后兼容",
  "modules": [
    { "name": "scalar_cache", "type": "CacheTLM",
      "params": { "size": "16KB", "assoc": 4, "line_size": 64 } },
    { "name": "l1_cache",     "type": "CacheTLM",
      "params": { "size": "32KB", "assoc": 8, "line_size": 64 } }
  ]
}
