# Legacy Modules (Port-based communication)

These modules use the legacy PortPair/PortManager communication model.
They are kept for backward compatibility with v2.0 projects.

New modules should be implemented using ChStream/StreamAdapter pattern
and placed in `../include/tlm/` instead.